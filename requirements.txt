# Pinned for reproducibility. Pipecat moves fast; these versions were the
# current stable line when this was written. Bump deliberately and re-test.
pipecat-ai[anthropic,assemblyai,cartesia,silero]==0.0.90
fastapi==0.118.0
uvicorn[standard]==0.37.0
twilio==9.4.1
gspread==6.1.4
python-dotenv==1.0.1
